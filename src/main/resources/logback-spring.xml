<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- 定义日志的上下文名称 -->
    <contextName>spring-boot-app</contextName>

    <!-- 日志的基本属性 -->
    <property name="LOG_PATH" value="logs" />
    <property name="LOG_FILE" value="${LOG_PATH}/application.log" />
    <property name="LOG_ARCHIVE" value="${LOG_PATH}/archive" />

    <!-- 控制台日志输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件日志输出，按天滚动，保留30天 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志文件名格式，按日期滚动 -->
            <fileNamePattern>${LOG_ARCHIVE}/application.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- 保留的历史日志文件的最大天数 -->
            <maxHistory>30</maxHistory>
            <!-- 每个日志文件的最大大小 -->
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 异常日志单独输出到 error.log 文件 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_ARCHIVE}/error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 定义日志的级别 -->
    <root level="INFO">
        <!-- 控制台输出 -->
        <appender-ref ref="CONSOLE" />
        <!-- 文件输出 -->
        <appender-ref ref="FILE" />
    </root>

    <!-- 错误级别日志单独记录到 error.log -->
    <logger name="ERROR_LOGGER" level="ERROR" additivity="false">
        <appender-ref ref="ERROR_FILE" />
    </logger>

    <!-- 其他自定义的包日志级别 -->
    <logger name="com.example" level="DEBUG" />

</configuration>